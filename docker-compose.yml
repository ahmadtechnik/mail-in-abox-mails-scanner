version: '3.8'

services:
  app:
    build: .
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - /home/user-data/mail/mailboxes:/mails:ro
      - .env:/app/.env
      - app.js:/app/app.js
    env_file:
      - .env
    user: mail
    restart: unless-stopped